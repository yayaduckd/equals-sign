using Assets.Modules.Interaction;
using UnityEngine;
using UnityEngine.Events;

public class DialogueInteractable : DefaultInteractable
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public override void Interact(UnityAction StopInteractionCallback)
    {
        base.Interact(StopInteractionCallback);

    }
}
